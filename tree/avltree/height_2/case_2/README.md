# AVL Insertion Puzzle — height 2, case 2

**Case:** right-unbalanced BST sequence (`right_unbalanced`)

**Sequence:** `10, 20, 30`

This sequence would produce a **height-2** binary search tree that is
**right unbalanced**. The same keys are inserted into
the AVL tree (which rebalances).

## Puzzle rules

- First ~30% of the execution-trace rows are pre-filled
- ~25% of all rows (chosen after that prefix) are blanks
- Blank columns: Line (`stepId`) only

## Stats

- Trace rows: **68**
- Blank rows: **17**

## Run

```bash
npx --yes serve avltree_insertion
```
